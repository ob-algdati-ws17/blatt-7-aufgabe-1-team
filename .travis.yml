language: cpp

compiler: gcc

install:
    - sudo apt-get update -y
    - sudo apt-get upgrade -y

script:
    - mkdir build
    - pwd
    - ls -lisa
    - cd build && cmake ..
    - cd build && make
    - cd build && make test